name: darc
description: "Dual Authentication and Riverpod Container - seamlessly manage Firebase and Supabase authentication with Riverpod integration"

version: 0.0.4
homepage: ''

environment:
  sdk: ^3.7.0
  flutter: ">=3.29.0"

dependencies:
  flutter:
    sdk: flutter

  firebase_auth: ^5.5.4
  firebase_core: ^3.13.1
  flutter_riverpod: ^2.6.1
  google_sign_in: ^6.3.0
  supabase_flutter: ^2.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
